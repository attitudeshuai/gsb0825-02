# 功能模块说明

## 1. 用户认证与权限模块

### 1.1 功能概述
提供安全的用户身份认证和细粒度的权限控制，确保不同角色的用户只能访问其权限范围内的功能。

### 1.2 核心功能

#### 用户登录
- 支持用户名密码登录
- JWT Token 认证机制
- Token 有效期 24 小时
- 登录失败次数限制
- 密码使用 bcrypt 加密存储

#### 角色权限
- **管理员 (admin)**: 拥有系统所有权限
  - 用户管理
  - 券批次创建/编辑/删除
  - 风控规则配置
  - 数据导出
  - 所有数据查看

- **运营专员 (operator)**: 运营相关权限
  - 券批次创建/编辑/激活
  - 券码生成/管理
  - 领取/核销记录查看
  - 数据导出
  - 不可删除批次、不可配置风控

- **财务人员 (finance)**: 财务相关权限
  - 所有数据查看
  - 数据导出
  - 财务分析报表
  - 不可创建/编辑数据

#### 个人中心
- 修改密码
- 查看个人信息
- 退出登录

### 1.3 默认账号
| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| admin | 123456 | 管理员 | 系统管理员，拥有全部权限 |
| operator | 123456 | 运营专员 | 运营人员，可管理券批次 |
| finance | 123456 | 财务人员 | 财务人员，可查看和导出数据 |

---

## 2. 券批次管理模块

### 2.1 功能概述
管理优惠券的生命周期，包括创建、配置、激活、取消等操作。

### 2.2 核心功能

#### 券类型支持
| 类型 | 标识 | 说明 |
|------|------|------|
| 满减券 | fixed | 满足消费门槛后减免固定金额，如满200减50 |
| 折扣券 | discount | 按比例折扣，如8折 |
| 直减券 | direct | 无门槛直接减免，如立减20元 |
| 兑换券 | redeem | 兑换特定商品或服务 |
| 运费券 | shipping | 抵扣订单运费 |

#### 批次创建
- 基础信息配置（名称、编码、描述）
- 券类型选择与面额配置
- 总数量设置
- 每人限领数量
- 有效期配置：
  - 固定时间段（如2024-01-01至2024-02-01）
  - 领取后N天有效
- 适用范围配置：
  - 全场通用
  - 指定商品分类
  - 指定商品
- 叠加规则配置：
  - 是否可与其他优惠券叠加
  - 叠加数量限制
- 实时预览配置效果

#### 批次列表
- 分页展示所有批次
- 按状态筛选（未开始/进行中/已结束/已取消）
- 按券类型筛选
- 关键词搜索（名称、编码）
- 显示库存进度条
- 显示领取率、核销率
- 快捷操作（查看、编辑、激活、取消）

#### 批次状态管理
- **未开始 (pending)**: 未到开始时间，不可领取
- **进行中 (active)**: 可正常领取和使用
- **已结束 (ended)**: 已过结束时间，不可领取
- **已取消 (cancelled)**: 手动取消，所有券码作废

#### 批量操作
- 批量激活批次
- 批量取消批次
- 批量导出批次数据

### 2.3 业务规则
- 已激活的批次不可修改核心配置（券类型、面额、数量等）
- 已结束的批次不可重新激活
- 取消批次会同时作废所有未使用的券码
- 批次编码全局唯一

---

## 3. 券码管理模块

### 3.1 功能概述
管理优惠券的券码生成、发放、状态追踪。

### 3.2 核心功能

#### 券码生成
- 批量生成券码
- 支持自定义前缀
- 券码长度可配置（6-16位）
- 生成类型：
  - 随机字符（字母+数字）
  - 纯数字
  - 字母数字混合
- 自动去重，确保券码唯一

#### 券码列表
- 分页展示券码
- 按批次筛选
- 按状态筛选（未领取/已领取/已使用/已过期/已作废）
- 券码搜索
- 显示领取用户、领取时间、使用时间

#### 券码状态管理
- **未领取 (available)**: 可被用户领取
- **已领取 (received)**: 已被用户领取，待使用
- **已使用 (used)**: 已核销使用
- **已过期 (expired)**: 超过有效期未使用
- **已作废 (cancelled)**: 手动作废

#### 券码导出
- 按筛选条件导出券码列表
- 导出格式：Excel
- 包含字段：券码、批次、状态、领取用户、领取时间、使用时间

### 3.3 业务规则
- 券码全局唯一
- 已领取的券码不可再次领取
- 已使用的券码不可作废
- 过期券码由定时任务自动处理

---

## 4. 领取与核销模块

### 4.1 功能概述
追踪优惠券的领取和使用情况，支持多种领取渠道和核销场景。

### 4.2 核心功能

#### 领取记录
- 展示所有领取明细
- 按批次筛选
- 按用户搜索（用户ID、手机号）
- 按领取渠道筛选
- 按时间范围筛选
- 显示领取时的IP和设备信息
- 支持批量作废未使用的券

#### 核销记录
- 展示所有核销明细
- 关联订单信息
- 显示订单金额、优惠金额
- 按批次、用户、时间筛选
- 支持退款退回（订单取消时券码退回）

#### 领取渠道
| 渠道 | 标识 | 说明 |
|------|------|------|
| APP | app | 移动端APP领取 |
| 小程序 | miniapp | 微信小程序等 |
| 网页 | web | H5或PC网页 |
| API | api | 通过接口发放 |
| 手动 | manual | 后台手动发放 |

#### 投放策略
- **主动发放**: 运营人员后台定向发放给指定用户
- **用户领取**: 用户主动领取
- **兑换码兑换**: 用户输入兑换码兑换
- **定向推送**: 根据用户标签推送
- **新人自动发放**: 新用户注册自动发放

### 4.3 业务规则
- 同一用户同一批次限领N张（可配置）
- 领取时校验库存，防止超发
- 核销时校验券码有效性和使用规则
- 订单退款时券码自动退回（如未过期）

---

## 5. 风控防刷模块

### 5.1 功能概述
防止恶意刷券行为，保护企业利益。

### 5.2 核心功能

#### 风控规则配置
- 单用户每小时领取限制
- 单用户每天领取限制
- 单用户总领取限制
- 单IP每小时领取限制
- 单设备每小时领取限制
- 异常检测开关
- 风险阈值设置

#### 黑名单管理
- IP黑名单
- 设备黑名单
- 用户黑名单
- 支持设置过期时间
- 支持手动添加和移除

#### 拦截记录
- 展示所有被拦截的请求
- 按拦截类型筛选
- 显示拦截原因、时间、IP、设备
- 可一键加入黑名单

#### 异常检测
- 基于行为模式识别异常领取
- 短时间内大量领取检测
- 多账号关联检测
- 异常IP聚类分析

### 5.3 风控规则示例
```json
{
  "maxReceivePerHour": 10,    // 每用户每小时最多领10张
  "maxReceivePerDay": 50,      // 每用户每天最多领50张
  "maxReceivePerUser": 3,      // 每批次每用户限领3张
  "ipMaxReceivePerHour": 100,  // 每IP每小时最多100张
  "deviceMaxReceivePerHour": 50 // 每设备每小时最多50张
}
```

---

## 6. 数据分析模块

### 6.1 功能概述
提供多维度的数据分析，帮助运营人员评估优惠券效果。

### 6.2 核心功能

#### 首页概览 (Dashboard)
- 关键指标卡片：
  - 券批次总数
  - 今日领取量
  - 今日核销量
  - 累计带动GMV
- 领取/核销趋势图（近7天）
- 券类型分布图
- 转化漏斗图
- 用户分层图

#### 趋势分析
- 领取量趋势（按天/周/月）
- 核销量趋势
- GMV趋势
- 支持按批次筛选
- 支持时间范围选择

#### 转化漏斗
- 曝光 → 点击 → 领取 → 使用 → 复购
- 各阶段转化率计算
- 独立用户数统计

#### ROI分析
- 累计GMV
- 累计优惠金额
- 整体ROI（GMV / 优惠金额）
- 按批次ROI排名

#### 用户分层
- 高价值用户：高消费频次+高客单价
- 中等价值用户：稳定消费
- 低价值用户：偶尔消费
- 新用户：最近30天注册
- 流失用户：超过90天未消费

#### 批次效果分析
- 各批次领取率、核销率排名
- 各批次GMV贡献
- 各批次ROI计算
- 支持按指标排序

### 6.3 关键指标计算公式

| 指标 | 公式 | 说明 |
|------|------|------|
| 领取率 | 已领取数量 / 总发放数量 × 100% | 反映券的吸引力 |
| 核销率 | 已使用数量 / 已领取数量 × 100% | 反映券的使用率 |
| ROI | 带动GMV / 优惠金额 | 投入产出比 |
| 客单价 | 订单总金额 / 订单数 | 使用券的用户消费能力 |
| 复购率 | 多次使用用户数 / 使用用户总数 | 用户粘性 |

---

## 7. 数据导出模块

### 7.1 功能概述
支持将各类数据导出为Excel格式，便于线下分析和汇报。

### 7.2 支持导出的数据

#### 批次数据导出
- 批次编码、名称、券类型
- 面额、使用门槛
- 总数量、已领取、已使用
- 领取率、核销率
- 状态、有效期
- 创建时间、创建人

#### 券码数据导出
- 券码、批次
- 状态、领取用户
- 领取时间、使用时间
- 关联订单号

#### 领取记录导出
- 批次、券码
- 用户、领取渠道
- 领取时间、IP、设备

#### 核销记录导出
- 批次、券码
- 用户、订单号
- 订单金额、优惠金额
- 核销时间

#### 效果分析导出
- 批次维度的效果数据
- 领取率、核销率、GMV、ROI
- 按时间汇总的趋势数据

### 7.3 导出特性
- 支持按筛选条件导出
- Excel格式（.xlsx）
- 大文件分批导出
- 导出进度提示

---

## 8. 系统定时任务

### 8.1 券过期回收
- **执行频率**: 每天凌晨0点
- **功能**: 
  - 将超过有效期的券码标记为过期
  - 更新批次统计数据
  - 释放库存占用

### 8.2 批次状态更新
- **执行频率**: 每天凌晨0点
- **功能**:
  - 自动激活到达开始时间的批次
  - 自动结束已过结束时间的批次

### 8.3 数据统计
- **执行频率**: 每小时
- **功能**:
  - 预计算统计数据
  - 更新缓存
  - 提升查询性能

---

## 9. 技术特性

### 9.1 高并发处理
- 数据库事务保证原子性
- 乐观锁防止超发
- 连接池管理
- 请求限流

### 9.2 数据安全
- 密码bcrypt加密
- JWT Token认证
- SQL注入防护
- XSS防护
- 敏感数据脱敏

### 9.3 性能优化
- 数据库索引优化
- 统计数据预计算
- 响应式前端设计
- 接口响应时间<500ms

### 9.4 可扩展性
- 模块化架构
- 前后端分离
- Docker容器化部署
- 支持水平扩展
